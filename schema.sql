CREATE TABLE IF NOT EXISTS Movies (
    Series_Title text PRIMARY KEY,
    Rating float,
    Released_Year integer,
    Overview text,
    Genre varchar (50),
    Star1 varchar (50),
    Star2 varchar (50),
    Star3 varchar (50),
    star4 varchar (50)
);

